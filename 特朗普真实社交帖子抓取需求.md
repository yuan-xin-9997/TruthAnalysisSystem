从 已抓取.md中读取数字，记为max

for x in range(max, max+100)，如果访问 https://trumpstruth.org/statuses/x 没有返回404，则：

1. 从 https://trumpstruth.org/statuses/x 抓取所有内容（包括但不限于文字、图片、视频），并保存在Markdown文件中，文件命名为日期+抓取内容的标题；将Markdown文件存放在 对应年份/对应月份目录下，如果对应年份和对应月份目录不存在，则新建该目录。

循环结束后，将没有返回404的URL中x最大的数字 将 已抓取.md 中的数字覆盖。

注意：1.保存的MarkDown文件名不能有特殊字符，如果有需要用英文替换
